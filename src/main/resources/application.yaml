server:
    port: 8083
management:
    endpoints:
        web:
          exposure:
              include: "health"
        endpoint:
            health:
              show-details: NEVER
http:
    request: "http://localhost:8081/v1/verify/keys"
