server:
    port: 8083
management:
    endpoints:
        cors:
          allowed-origins: "*"
        web:
          exposure:
              include: "health"
        endpoint:
            health:
              show-details: NEVER
http:
    request:
      verify: "http://localhost:8081/v1/verify/keys"
      stack: "http://localhost:8081/v1/orange/create"
      status-infra: "http://localhost:8081/v1/orange/status"
